// /*
// *
// *       DO NOT EDIT THIS FILE
// *       For FCC testing purposes!
// *
// */

// const EventEmitter = require('events').EventEmitter;
// const Mocha = require('mocha');
// const fs = require('fs');
// const path = require('path');

// const mocha = new Mocha({ timeout: 10000 });
// const testDir = './tests';

// try {
//   fs.readdirSync(testDir).filter(function(file) {
//     return file.substr(-3) === '.js';
//   }).forEach(function(file) {
//     mocha.addFile(
//       path.join(testDir, file)
//     );
//   });
// } catch(e) {
//   console.log('Error loading test files:', e);
// }

// const runner = new EventEmitter();
// runner.tests = [];

// runner.run = function() {
//   try {
//     mocha.ui('tdd').run()
//       .on('pass', function(test) {
//         runner.tests.push({
//           title: test.title,
//           suite: test.suite ? test.suite.title : '',
//           states: 'passed',
//           body: test.body
//         });
//       })
//       .on('fail', function(test, err) {
//         runner.tests.push({
//           title: test.title,
//           suite: test.suite ? test.suite.title : '',
//           states: 'failed',
//           error: err.message,
//           body: test.body
//         });
//       })
//       .on('end', function() {
//         runner.emit('done', runner.tests);
//       });
//   } catch(e) {
//     throw(e);
//   }
// };

// module.exports = runner;

/*
*       DO NOT EDIT THIS FILE
*       For FCC testing purposes!
*/

const EventEmitter = require('events').EventEmitter;
const Mocha = require('mocha');
const fs = require('fs');
const path = require('path');

const mocha = new Mocha({ timeout: 10000 });
const testDir = './tests';

try {
  fs.readdirSync(testDir).filter(function(file) {
    return file.substr(-3) === '.js';
  }).forEach(function(file) {
    mocha.addFile(
      path.join(testDir, file)
    );
  });
} catch(e) {
  console.log('Error loading test files:', e);
}

const runner = new EventEmitter();
runner.tests = [];

runner.run = function() {
  try {
    mocha.ui('tdd').run()
      .on('pass', function(test) {
        runner.tests.push({
          title: test.title,
          suite: test.suite ? test.suite.title : '',
          state: 'passed',
          states: 'passed',
          body: test.body
        });
      })
      .on('fail', function(test, err) {
        runner.tests.push({
          title: test.title,
          suite: test.suite ? test.suite.title : '',
          state: 'failed',
          states: 'failed',
          error: err.message,
          body: test.body
        });
      })
      .on('end', function() {
        runner.emit('done', runner.tests);
      });
  } catch(e) {
    throw(e);
  }
};

module.exports = runner;